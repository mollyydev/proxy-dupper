[project]
name = "proxy-dupper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.13.3",
    "httpx[socks]>=0.28.1",
    "loguru>=0.7.3",
]
